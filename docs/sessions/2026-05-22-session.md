# Session: 2026-05-22
**Project:** Claude Code Setup (global + Apple-level configuration)
**Duration:** ~1 session

## What was done

### Apple folder consolidation
- Created `/Work/Apple/.claude/agents/` and `/Work/Apple/.claude/skills/`
- Moved `swift-reviewer`, `ui-reviewer`, `xcode-build` (generic) from all 4 individual projects → Apple folder level
- Moved `swiftui-patterns`, `swiftui-navigation`, `swiftui-performance` from `~/.claude/skills/` → `Apple/.claude/skills/`
- Moved `swiftdata-pro`, `swift-concurrency-pro` from `/Work/.agents/skills/` (global) → `/Work/Apple/.agents/skills/` with matching `skills-lock.json`
- Cleared `/Work/skills-lock.json` (now empty — no longer leaks Swift skills into Web/Arduino projects)

### Apple-level agent upgrades (major rewrites)
- **`swift-reviewer`** — full rewrite: Swift 6 strict concurrency, `@Observable` isolation rules, severity scale, structured output format
- **`ui-reviewer`** — full rewrite: iOS 26 Liquid Glass rules, macOS Tahoe chrome, SF Symbols 6, `.tint` vs `.accentColor`, semantic colors, iPad split view, empty state requirements
- **`xcode-build`** — full rewrite: Swift 6 migration error glossary, `PrivacyInfo.xcprivacy` requirements, Xcode 26 archive/distribution issues, SourceKit false-positive detection

### New at Apple level
- **`privacy-reviewer` agent** — `PrivacyInfo.xcprivacy` audit, `NS*UsageDescription` quality, App Store privacy label accuracy, third-party SDK manifest check, data minimisation
- **`swiftui-layout-components` skill** — stacks, grids, lists, scroll views, forms, `.searchable`, `ViewThatFits`, `Layout` protocol, iOS 26 container patterns (was a missing sibling referenced by `swiftui-patterns`)

### Global agent upgrades (targeted)
- **`code-reviewer`** — added: check `.claude/rules/review-quality.md` before reviewing (project-local rules pattern)
- **`release-checklist`** — iOS Privacy section now invokes `privacy-reviewer` agent; expanded to match full audit scope
- **`accessibility-reviewer`** — added `.accessibilityInputLabels`, `.accessibilityActivationPoint`, gesture alternatives, iOS 26 glass contrast, reduced-motion gating
- **`dependency-auditor`** — Swift PM section now uses swiftpackageindex.com; checks for `PrivacyInfo.xcprivacy` in packages

### Global skill fixes
- **`tdd`** — removed 5 dead internal links (`tests.md`, `mocking.md`, `deep-modules.md`, `interface-design.md`, `refactoring.md`); inlined the key guidance
- **`security-review`** — replaced Solana section with Ethereum/Solidity (viem/wagmi); added iOS Swift section (Keychain, ATS, log redaction); updated pre-deployment checklist

## Current state

All configuration changes are live. The Apple folder now has a complete, self-contained `.claude/` setup:
- 4 agents: `swift-reviewer`, `ui-reviewer`, `xcode-build`, `privacy-reviewer`
- 4 skills: `swiftui-patterns`, `swiftui-navigation`, `swiftui-performance`, `swiftui-layout-components`
- 2 skills via `skills-lock.json`: `swiftdata-pro`, `swift-concurrency-pro`

Global agents (12) and global skills (21) are unchanged in count but improved in quality.

Project-level agents that were customized (Finly `xcode-build`, Drinking Reminder `swift-reviewer` + `xcode-build`) correctly override the Apple-level generics. No changes to project-level customizations.

## In progress / next action
- [ ] Open each Apple project and verify Apple-level agents load correctly (`/swift-reviewer`, `/privacy-reviewer` etc.)
- [ ] Run `privacy-reviewer` on Drinking Reminder and Finly before next App Store submission
- [ ] Consider updating Finly and Drinking Reminder CLAUDE.md files to mention `privacy-reviewer` is now available

## Blockers
- None

## Open decisions
- None

## Commits this session
None — configuration-only session, no source code changed.

## Notes
- `swiftdata-pro` and `swift-concurrency-pro` were in `/Work/.agents/skills/` (not `~/.agents/skills/`), discovered via `skills-lock.json` at `/Work/` level. After move, they live in `/Work/Apple/.agents/skills/` with a matching `Apple/skills-lock.json`.
- The `swiftui-patterns` skill already referenced `swiftui-layout-components` as a sibling — confirmed it was genuinely missing, created it.
- No new global agents or skills created (coverage was already complete — additions would have caused overlap).
- `security-review` was ECC-origin and had hardcoded Solana references; user's blockchain project (payo) is Ethereum. Fixed.
