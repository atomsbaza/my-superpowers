# Session: 2026-05-27
**Project:** Claude Code Setup (global + Apple projects)
**Duration:** ~1 session

## What was done

**Skills cleanup (global):**
- Removed 9 redundant/rarely-used global skills: `dispatching-parallel-agents`, `subagent-driven-development`, `improve-codebase-architecture`, `management-talk`, `post-mortem`, `handoff`, `writing-skills`, `receiving-code-review`
- Moved `swift-concurrency-expert` from global → `/Work/Apple/.claude/skills/` (correct scope)
- Global skills: 32 → 23

**Configuration improvements:**
- Created `~/.claude/rules/improvement-loop.md` — directive to save any correction to memory immediately, not at end of session
- Added `paths: ["**/*.swift"]` frontmatter to `conventions.md` in all 4 Apple projects — Swift coding rules now load only when editing Swift files
- Created `/Work/Apple/CLAUDE.md` — shared context for all Apple projects: DEVELOPER_DIR build pattern, SourceKit gotcha, Swift 6 baseline, available agents and skills

**Stale reference cleanup:**
- Deleted `~/.claude/skills/9ARM_SKILLS_INTEGRATION.md` — stale adoption guide for removed skills
- Removed `post-mortem` and `management-talk` references from Drinking Reminder CLAUDE.md
- Removed entire "iOS Development Workflows" section from Drinking Reminder CLAUDE.md (−77 lines, workflow diagrams duplicated global memory)
- Deleted stale `ui-reviewer.md` from Drinking Reminder project — Apple-level version (more comprehensive, iOS 26 / Liquid Glass) now takes over
- Fixed 8 global agents with broken `post-mortem` / `management-talk` skill references: `debugger`, `silent-failure-hunter`, `test-writer`, `doc-updater`, `wiki-updater`, `pr-description`, `docs-writer`, `release-checklist`

**Directory scaffolding:**
- Created `docs/sessions/` in CapsLockNoDelay and Finly — session summaries now persist instead of printing inline

## Current state

Claude Code global setup and all 4 Apple project configurations are clean and consistent. No stale skill references remain anywhere in agents, rules, or CLAUDE.md files.

## In progress / next action
- [ ] None — setup is in good shape. Resume normal project work.

## Blockers
- None

## Open decisions
- `docs/research/` is absent from all 4 Apple projects — research saves to top-level `/Work/docs/research/` instead. Acceptable as a centralized store; create per-project dirs only if per-project research history is wanted.

## Commits this session
None — all changes were to config files outside version-controlled project directories.

## Notes
- The improvement loop rule (`~/.claude/rules/improvement-loop.md`) is new behavior: Claude should now save corrections to memory immediately rather than opportunistically. Watch for it in future sessions.
- Path-scoped rules require reading a `.swift` file to trigger — rules won't load if a session is purely docs/config work in an Apple project.
- Apple CLAUDE.md stacks with project-level CLAUDE.md files automatically via Claude Code's hierarchical loading.
