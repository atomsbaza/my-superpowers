# Session: 2026-05-29
**Project:** Global Claude Code Skills / Tooling (Kiro Research)
**Duration:** ~2 hours

## What was done
- `/research` on **Kiro CLI experimental features** — all 6 features (Knowledge Management, Tangent Mode, TODO Lists, Thinking Tool, Checkpointing, Context Usage %) with full command reference, setting names, and "classic only" scope notes
- `/research` on **Kiro multi-root workspaces** — setup via drag-and-drop or File menu, steering/hooks/MCP scoping per root, hard limitation: Repo Indexing incompatible with multi-folder workspaces
- `/research` on **Kiro multi-agent workflows** — full subagents architecture (DAG, 4-concurrent cap, context isolation), custom agent config (IDE markdown vs CLI JSON), 4 orchestration patterns (fan-out, DAG, player/coach, spec-driven multi-repo), open issues: hooks don't fire in subagents (#7755), power steering not inherited (#7758)
- `/research` on **requix/kiro-team** — copy-in config set implementing plan→build→validate→document loop using git worktrees for isolation; 4 specialized agents with tool restrictions; requires Kiro CLI 2.0+
- `/research` on **Kiro hooks automation** — comprehensive reference: all 10 trigger types, full schema, 10 hooks from blog post, verbatim hooks from awsdataarchitect/kiro-best-practices, community patterns (preToolUse blocking, postTaskExecution, deploy assistant), 5 community repos listed

All 5 reports saved to `docs/research/kiro/` as untracked files (not yet committed).

## Current state
Repo has 5 untracked research files ready to commit. No code was modified. All research is self-contained markdown reports. The 5 files are fully written and accurate.

## In progress / next action
- [ ] `git add docs/research/kiro/2026-05-29-*.md && git commit -m "docs: add Kiro research reports for experimental features, multi-root, multi-agent, hooks, and kiro-team"`
- [ ] Consider adopting `requix/kiro-team` in a project: copy `.kiro/agents/` and `scripts/` into a project root, enable `chat.enableTodoList true`
- [ ] Enable useful hooks per project: at minimum `Security Scanner` (agentStop) and `Lint and Format on Save` (fileEdited)

## Blockers
- None

## Open decisions
- Which of the 6 experimental features to enable globally vs per-project? Knowledge Management and Thinking Tool are not classic-only (safe to use in CLI); the other 4 are classic-only.
- `kiro-team` runs tasks sequentially within a spec — worth evaluating vs the AWS sample (`aws-samples/sample-kiro-cli-multiagent-development`) which runs 6 agents with more parallelism.

## Commits this session
```
3148def docs: update session summary with Kiro CLI research and final state  (prior session)
```
*(No new commits yet — 5 files are untracked)*

## Notes
- **Critical cross-cutting finding:** Hooks do NOT fire inside subagents (open issue #7755). Any hook-based governance (security scans, enforcement) is bypassed entirely when using multi-agent workflows. Design safety rules accordingly.
- **4 of 6 experimental CLI features are "classic only"** — they don't work in the terminal UI variant of Kiro CLI. Only Knowledge Management and Thinking Tool work in both.
- **kiro-team MCP working directory trap:** All MCP servers launched by kiro-team use the first root's directory as working directory regardless of where they're defined. Use absolute paths in MCP configs.
- **No Logseq page found for this project** — wiki not updated.
